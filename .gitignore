# Secrets — never commit
.env
.env.local
.env.*.local
!.env.example

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*$py.class
.Python
*.so

# Local runtime / user data (do not push)
data/conversations/conversations.json
data/midterm_memory.json
data/artifacts/
data/methodology/methodologies.json
data/methodology/category_feedback.json

# Keys / certs (if ever added)
*.pem
*.key
id_rsa
id_ed25519

# IDE / OS
.cursor/
.claude/
.idea/
.vscode/
*.swp
Thumbs.db
.DS_Store

# Accidental copies of user Desktop under repo root (e.g. drag-dropped folder)
桌面/

# Mis-redirected shell/pip output (e.g. `>=2.0.0` creating a file named `=2.0.0`)
/=*.0

# Third-party downloaded repos (not dependencies)
DeepSeek-OCR-2-main/
agency-agents-main/
third_party/agency-agents/

# Test cache
.pytest_cache/
